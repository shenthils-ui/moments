# Optional fixtures

Put a real iPhone HEIC here as `sample.heic` to exercise the full HEIC
thumbnail path (WASM libheif decode) in `npm test`. Without it that one
test is skipped; the placeholder path is always tested.
